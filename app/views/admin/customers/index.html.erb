<div class="container">
  <h3 class="my-5 text-center">会員一覧</h3>
    <div class="row d-flex justify-content-center">
      <!--td幅が広すぎる&行すべてがリンクになるようにしたい（商品一覧も同様に）-->
      <table class="table table-hover w-50 text-center">
        <thead>
          <tr>
            <th class="col-1">ID</th>
            <th class="col-2">氏名</th>
            <th class="col-2">ステータス</th>
          </tr>
        </thead>
        <tbody>
          <% @customers.each do |customer| %>
          <tr>
            <td><%= customer.id %></td>
            <td><%= link_to customer.last_name + " " + customer.first_name, admin_customer_path(customer.id), class: "text-decoration-none" %></td>
            <td><% if customer.is_deleted %>
                  退会
                <% else %>
                  有効
                <% end %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>