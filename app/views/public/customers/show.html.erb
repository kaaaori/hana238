<div class="container">
  <h3 class="my-5 text-center">My page</h3>
  <div class="row d-flex justify-content-center">
    <!--下線を短く-->
    <div class="mt-5 me-5 col-md-3 d-none d-md-block"><!--mdより狭い画面では非表示-->
      <%= render 'sidebar' %>
    </div>
    <!--右側-->
    <div class="col-md-5">
      <h5 class="text-center mt-3">登録情報</h5>
      <div class="row d-flex justify-content-center">
        <table class="table table-borderless", border="1">
          <tr>
            <td class="col-2">氏名</td>
            <td class="col-3"><%= @customer.last_name %>　<%= @customer.first_name %></td>
          </tr>
          <tr>
            <td bgcolor="#f5f5f5">メールアドレス</td>
            <td><%= @customer.email %></td>
          </tr>
          <tr>
            <td bgcolor="#f5f5f5">電話番号</td>
            <td><%= @customer.phone_number %></td>
          </tr>
          <!--テキスト右寄せにしたい＆リンク下線なしにしたい-->
          <tr>
            <td></td>
            <td class="text-right"><%= link_to "編集する", customers_info_edit_path %></td>
          </tr>
        </table>
      </div>
    </div>
  </div>
</div>