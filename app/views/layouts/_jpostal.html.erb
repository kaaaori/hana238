<script>
  $('#postal_code').jpostal({
    postcode : [
      // 取得する郵便番号のテキストボックスをidで指定
      '#postal_code'
    ],
    address: {
      // %3 => 都道府県、 %4 => 市区町村 %5 => 町域 %6 => 番地 %7 => 名称
      // それぞれを表示するコントロールをidで指定
      "#prefecture_code"  : "%3",
      "#address_city"   : "%4%5",
      "#address_street" : "%6%7"
    }
  });
</script>